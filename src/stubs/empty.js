// Stub module: this project only uses the TensorFlow.js (MoveNet) runtime
// from @tensorflow-models/pose-detection, never the @mediapipe/pose runtime,
// but the package statically imports it. This empty stub satisfies the
// bundler without shipping the unused mediapipe runtime.
module.exports = {};
